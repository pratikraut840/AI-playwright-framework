# Dependencies
node_modules
/node_modules

# Test results & reports (do not commit)
test-results/
junit-report-api/
test-output.txt

# Allure (all under allure-report/)
allure-report/

# Playwright
playwright-report/
playwright/.cache/
playwright/.auth/
blob-report/
*.trace

# Auth & session state (contains cookies; regenerate via globalSetup)
src/ui/helpers/setupLogin/auth/admin-user.json

# Teardown artifact
.last-run.json

# IDE & OS
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment (copy from .env.example; do not commit secrets)
src/api/env/.env
src/api/config/.env.api
src/ui/helpers/env/.env

# Build / coverage
/dist
/build
/coverage
*.tsbuildinfo

# Misc
**/*.test.ts
.env.local
.env.*.local

# Excel lock files (Windows)
~$*

# Cursor IDE (optional)
.cursor/
